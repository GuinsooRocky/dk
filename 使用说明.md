# chat-cc-bot 使用说明

> 在飞书/微信里发消息 → 在你自己的 Mac 上跑 Claude Code → 结果回到对话。
> 这份是"从零到能用"的总入口；细节分别见 [`feishu-claude/README.md`](./feishu-claude/README.md)、[`wechat-claude-bot/README.md`](./wechat-claude-bot/README.md)、[`SANDBOX.md`](./SANDBOX.md)、[`daemon/README.md`](./daemon/README.md)。

---

## 0. 前提

- 本机装好并**登录过** Claude Code（终端能跑 `claude`）。验证：`claude --version`。
- Python 3.12+（飞书侧依赖在 `feishu-claude/.venv` 里）。
- Mac 要**开机、不休眠**机器人才在线（合盖睡眠就断，这是物理限制，见 §7）。
- ⚠️ 跑之前先读 §8 安全红线。

---

## 1. 飞书机器人 · 5 步上手（推荐 WS 长连接，**不用 ngrok**）

### 1.1 建飞书应用（一次性）
1. https://open.feishu.cn → 开发者后台 → 创建**企业自建应用**
2. 「添加应用能力」→ 启用**机器人**
3. 「权限管理」开通：`im:message`、`im:message:send_as_bot`、`im:message.group_at_msg`
4. 「凭证与基础信息」拿到 **App ID / App Secret**
5. 「事件订阅」→ 添加事件 `接收消息 v2.0`(`im.message.receive_v1`)，**订阅方式选「使用长连接接收事件」**（关键：选了这个就不用填任何 URL / 不用 ngrok）
6. 发布版本（个人租户秒过），把机器人**拉进一个群**

### 1.2 配 .env
```bash
cd ~/Desktop/my-code/chat-cc-bot/feishu-claude
cp .env.example .env      # 已存在就跳过
# 编辑 .env，至少填：
#   FEISHU_APP_ID=cli_xxx
#   FEISHU_APP_SECRET=xxx
#   ALLOWED_USERS=        ← 先留空，第 1.4 步跑通后从日志拿 open_id 回填
```

### 1.3 装依赖
```bash
cd ~/Desktop/my-code/chat-cc-bot
feishu-claude/.venv/bin/python -m pip install -r feishu-claude/requirements.txt
```
> 注：`.venv` 的 pip 有个旧路径 shebang，所以用 `python -m pip` 而不是直接 `pip`。

### 1.4 启动（先前台测，确认 OK 再上守护）
```bash
feishu-claude/.venv/bin/python feishu-claude/feishu_ws_server.py
```
看到 `Feishu WS 长连接启动` 即连上。这时**白名单还空着 → 机器人会拒绝所有人**。

### 1.5 拿到自己的 open_id 并回填
群里 @ 机器人发一句话 → 看终端日志里的 `拒绝非白名单 from=ou_xxxx` → 把那个 `ou_xxxx` 填进 `.env` 的 `ALLOWED_USERS=ou_xxxx` → **重启**。再 @ 机器人，应回「思考中…」+ 答案。✅

---

## 2. 守护进程（开机自启 + 崩溃自愈，不用一直开终端）

确认 §1 前台能跑通后：
```bash
./daemon/feishu-daemon.sh install     # 装成 launchd 服务，登录自启、崩了自拉
./daemon/feishu-daemon.sh status      # 看状态/PID/上次退出码
./daemon/feishu-daemon.sh logs        # 跟踪日志
./daemon/feishu-daemon.sh restart     # 改了 .env 后重启生效
./daemon/feishu-daemon.sh stop        # 停（必须用它，光 kill 会被自动拉起）
./daemon/feishu-daemon.sh uninstall   # 彻底移除，不再自启
```

---

## 3. 微信机器人（可选，有封号风险用小号）
```bash
cd ~/Desktop/my-code/chat-cc-bot/wechat-claude-bot
./setup.sh                       # 装 Node/Python 依赖
cp .env.example .env             # 填 ALLOWED_USERS=你的微信昵称（留空=拒绝所有）
# 两个终端：
python3 claude_bridge.py         # 终端1：桥接服务
node wechat_bot.js               # 终端2：扫码登录微信
```
触发：私聊发 `/c 你的问题`、或群里 @机器人、或给「文件传输助手」发任意消息。
> 微信用 web 协议，可能掉线/封号；建议专用小号。稳定方案见 `wechat-claude-bot/README.md` 的企业微信段。

---

## 4. 日常使用

- **怎么跟它说话**：飞书——群里 `@机器人 你的问题`，或私聊机器人直接发；微信——见 §3。机器人**没有网页地址**，就在 IM 里 @ 它。
- **多轮对话**：同一个会话（飞书按群、微信按人）自动续上下文，它记得你上一句。
- **正在忙**：默认单飞（`CLAUDE_MAX_CONCURRENCY=1`），上一条还在跑时再发会回「⏳ 正在处理上一条」，等它完再发。
- **长回答**：自动分段发送。
- **单向推送**（不用 @，从终端/定时任务推结果到群）：
  ```bash
  feishu-claude/.venv/bin/python feishu-claude/feishu_send.py "帮我总结今天的科技新闻"
  echo "今日待办" | feishu-claude/.venv/bin/python feishu-claude/feishu_send.py
  ```
  （需在 `.env` 配 `FEISHU_WEBHOOK_URL`，即飞书群里「添加自定义机器人」拿到的 webhook）

---

## 5. `.env` 配置速查

| 变量 | 说明 | 默认 |
|---|---|---|
| `FEISHU_APP_ID` / `FEISHU_APP_SECRET` | 飞书应用凭证 | 必填 |
| `ALLOWED_USERS` | 白名单 open_id（飞书）/昵称（微信），逗号分隔。**留空=拒绝所有人** | 空（fail-closed） |
| `CLAUDE_TOOLS` | 允许的工具。默认只读，开 `Bash/Write/Edit` 见 §8 | `Read,Glob,Grep,WebFetch` |
| `CLAUDE_MAX_CONCURRENCY` | 同时最多几个 claude（1=单飞，防爆内存） | `1` |
| `CLAUDE_ENGINE` | `cli`（默认，subprocess）/ `sdk`（进程内 Agent SDK） | `cli` |
| `CLAUDE_SETTINGS` | sandbox 配置文件路径（见 §6 / SANDBOX.md） | 空 |
| `CLAUDE_TIMEOUT` | 单次任务超时秒 | `300` |
| `CLAUDE_CMD` | claude 可执行路径（不在 PATH 时填绝对路径） | `claude` |
| `FEISHU_VERIFY_TOKEN` / `FEISHU_ENCRYPT_KEY` | webhook 鉴权/加密（WS 模式可不填） | 空 |
| `FEISHU_BOT_OPEN_ID` | 机器人自身 open_id（留空自动获取） | 空 |

> 改完 `.env` 要**重启**服务（前台 Ctrl+C 重跑，或 `./daemon/feishu-daemon.sh restart`）。

---

## 6. 进阶

**换进程内引擎**：`.env` 设 `CLAUDE_ENGINE=sdk` + `pip install claude-agent-sdk`。默认 `cli` 已够用且最稳。

**开 Bash 并保证安全**（真要让它执行命令时）：
```bash
cp sandbox-settings.example.json sandbox-settings.json   # 改 allowWrite 为你的工作目录
# .env 设：
#   CLAUDE_TOOLS=Read,Glob,Grep,WebFetch,Bash,Write,Edit
#   CLAUDE_SETTINGS=/Users/你/Desktop/my-code/chat-cc-bot/sandbox-settings.json
```
sandbox 会把写入限在工作目录、禁读 `~/.ssh` 等、限制网络。详见 [SANDBOX.md](./SANDBOX.md)。
> 不开 Bash（默认）时不需要 sandbox。

---

## 7. 常见问题

| 现象 | 排查 |
|---|---|
| 机器人不理我 | ① WS 模式：飞书后台订阅方式是不是「长连接」② 你的 open_id 在 `ALLOWED_USERS` 里吗（看日志拿真实 id）③ 群里有没有真的 @ 到机器人 |
| 日志全是「拒绝非白名单」 | `ALLOWED_USERS` 没配或配错，按 §1.5 回填 |
| 一直「思考中」不出答案 | claude 超时（调大 `CLAUDE_TIMEOUT`）/ claude 没登录 / `CLAUDE_CMD` 路径不对 |
| 找不到 claude 命令 | `.env` 设 `CLAUDE_CMD=/Users/你/.local/bin/claude` |
| 守护进程关不掉 | 用 `./daemon/feishu-daemon.sh stop`（KeepAlive 服务光 kill 会被拉起） |
| 合盖后断了 | 物理限制：launchd/caffeinate 救不了休眠。要 7×24 在系统设置关休眠或插电夹盖运行 |
| 改了 .env 没生效 | 重启服务 |

---

## 8. 安全红线（务必看）

- **默认工具只读**，不开 Bash。开 Bash = 任何能给机器人发消息的人能在你 Mac 上执行命令——要开**必须**配 §6 的 sandbox。
- **白名单 fail-closed**：`ALLOWED_USERS` 空就拒绝所有人，别图省事留空放开。
- **`CLAUDE_WORK_DIR` 不是沙箱**，只是初始目录；真隔离靠 sandbox。
- **secrets 不入库**：`.env` 已被 `.gitignore` 挡住，别手动加进 git。
- App Secret 若曾明文存在过，去飞书后台重新生成一次。
